export default {
  // Source content directories
  srcDir: './src',
  outDir: './docs',
  // Directories of source files, relative to scrDir as root
  postsDir: '/posts',
  pagesDir: '/pages',
  dataDir: '/data',
  assetsDir: '/assets',
  stylesDir: '/sass',
  layoutDir: '/layouts',
  // Render Settings
  usePermalinks: true,
}
